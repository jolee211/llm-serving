apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: llm-serving
  region: us-east-1

managedNodeGroups:
  - name: gpu-spot
    instanceTypes: ["g5.xlarge", "g6.xlarge", "g5.2xlarge", "g6.2xlarge"]
    spot: true
    desiredCapacity: 1
    minSize: 0
    maxSize: 2
    volumeSize: 100
    labels:
      workload: gpu
    taints:
      - key: nvidia.com/gpu
        value: "true"
        effect: NoSchedule
